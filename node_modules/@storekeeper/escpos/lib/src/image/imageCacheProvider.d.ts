import { RasterImage } from './rasterImage';
export declare abstract class ImageCacheProvider {
    abstract set(key: string, image: RasterImage): void | Promise<void>;
    abstract get(key: string): undefined | RasterImage | Promise<undefined | RasterImage>;
}
export declare let imageCacheProvider: ImageCacheProvider | null;
export declare const setImageCacheProvider: (cacheProvider: ImageCacheProvider) => void;
