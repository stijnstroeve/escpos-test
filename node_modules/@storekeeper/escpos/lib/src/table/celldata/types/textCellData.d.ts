import { CellData } from '../cellData';
import { TableColumnDefinitionType, TableRow } from '../../../escpos/commands/tableCommand';
import { TableCompiler } from '../../tableCompiler';
export declare class TextCellData extends CellData<string> {
    constructor(text: string);
    getTextLines<T, K extends keyof T>(row: TableRow<T>, column: TableColumnDefinitionType<T, K>, compiler: TableCompiler): string[];
}
