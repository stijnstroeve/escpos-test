import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
export declare type BarcodeWidth = 1 | 2 | 3;
export declare type BarcodeOptions = {
    width?: BarcodeWidth;
    height?: number;
};
export declare class BarcodeCommand extends Command {
    barcode: string;
    options: BarcodeOptions;
    key: string;
    constructor(barcode: string, options: BarcodeOptions);
    build(compiler: EscPosCompiler): number[];
}
