import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
import { AlignHTOptions } from '../escposBuilder';
export declare type QROptions = {
    size?: number;
} & AlignHTOptions;
export declare class QRCommand extends Command {
    text: string;
    options: QROptions;
    key: string;
    constructor(text: string, options: QROptions);
    build(compiler: EscPosCompiler): number[];
}
