import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
export declare class LineSpacingCommand extends Command {
    spacing?: number;
    key: string;
    constructor(spacing?: number);
    build(compiler: EscPosCompiler): number[];
}
