import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
import { AlignHTOptions } from '../escposBuilder';
export declare type ImageOptions = {
    width?: number;
    height?: number;
    disableImageDithering?: boolean;
} & AlignHTOptions;
export declare class ImageCommand extends Command {
    url: string;
    options: ImageOptions;
    key: string;
    constructor(url: string, options: ImageOptions);
    build(compiler: EscPosCompiler): Promise<number[]>;
}
