import { Device, IDeviceInfo } from './device';
export declare type DummyDeviceData = {
    isDummy: boolean;
};
export declare class DummyDevice extends Device<DummyDeviceData> {
    private data;
    constructor();
    close(): void;
    open(): void;
    write(data: Uint8Array, cb?: (err?: any) => void): void;
    getData(): number[];
    getDataInUInt8Array(): Uint8Array;
    /**
     * Resets the data present in the dummy printer.
     */
    reset(): void;
    /**
     * Note: This function returns static data because its a dummy.
     */
    getDeviceInfo(): IDeviceInfo<DummyDeviceData>;
}
