import { AlignHTOptions } from '../escpos/escposBuilder';
export declare type RasterImageOptions = AlignHTOptions;
export declare type RasterImage = {
    data: Uint8Array;
    width: number;
    height: number;
    rasterWidth: number;
};
export declare class RasterImageGenerator {
    /**
     * Converts the given bitmap into a raster image.
     * @param blackWhiteBitmap
     * @param width
     * @param height
     * @private
     */
    static fromBitmap(blackWhiteBitmap: Uint8Array, width: number, height: number): RasterImage;
    static getRasterWidth(width: number): number;
}
