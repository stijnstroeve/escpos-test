import { Device } from '../device';
import { EscPosBuilder, EscPosPrinterOptions } from '@storekeeper/escpos';
export declare type BuildAndPrintOptions = {
    /**
     * Close the device after the command has been printed.
     */
    close?: boolean;
};
export declare class EscPosPrinter<DeviceData> {
    private readonly device;
    private readonly options;
    constructor(device: Device<DeviceData>, options?: EscPosPrinterOptions);
    writeCommand(command: Uint8Array, cb?: (err?: any) => void): void;
    buildAndPrint(builder: EscPosBuilder, options?: BuildAndPrintOptions): Promise<void>;
    close(): EscPosPrinter<DeviceData>;
}
