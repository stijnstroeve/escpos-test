import { EscPosCompiler } from '../escposCompiler';
export declare abstract class Command {
    abstract key: string;
    abstract build(compiler: EscPosCompiler): number[] | Promise<number[]>;
}
