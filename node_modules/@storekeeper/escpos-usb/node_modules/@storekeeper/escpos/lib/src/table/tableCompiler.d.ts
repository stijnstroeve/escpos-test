import { Table } from '../escpos/commands/tableCommand';
import { EscPosCompiler } from '../escpos/escposCompiler';
import { TextSizeOption } from '../escpos/commands/textCommand';
declare type TableCellStyle = {
    bold: boolean;
    underline: boolean;
    align: 'left' | 'right';
};
export declare type TableCell = {
    charWidth: number;
    originalRow: number;
    row: number;
    column: number;
    text: string;
    isLastTextLine: boolean;
    style: TableCellStyle;
};
export declare class TableCompiler {
    private compiler;
    constructor(compiler: EscPosCompiler);
    private getTextLinesFromCellData;
    private static createCellDataFromType;
    private isLastRowForTextLines;
    getColumnCharacterWidth(textSize: TextSizeOption, columnWidth: number): number;
    getTableCells<T, K extends keyof T>(table: Table<T, K>): TableCell[];
    compileTableHeaders<T, K extends keyof T>(table: Table<T, K>): number[];
    compileTableBody<T, K extends keyof T>(table: Table<T, K>): number[];
    compileTable<T, K extends keyof T>(table: Table<T, K>): number[];
}
export {};
