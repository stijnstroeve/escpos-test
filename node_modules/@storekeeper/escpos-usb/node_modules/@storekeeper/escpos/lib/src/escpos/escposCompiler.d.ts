import { TextOptions, TextSizeOption } from './commands/textCommand';
import { Table } from './commands/tableCommand';
import { BarcodeOptions } from './commands/barcodeCommand';
import { Image, ImageProvider } from '../image/image';
import { ImageOptions } from './commands/imageCommand';
import { LineOptions } from './commands/lineCommand';
export declare type CompilerOptions = {
    fontAMaxChars: number;
    fontBMaxChars: number;
    imageProvider?: ImageProvider;
    textEncoding?: string;
};
export declare class EscPosCompiler {
    options: CompilerOptions;
    constructor(options: CompilerOptions);
    text(text: string, options?: TextOptions): number[];
    getMaxCharsPerLine(size?: number): number;
    line(options?: LineOptions): number[];
    table<T, K extends keyof T>(table: Table<T, K>): number[];
    barcode(barcode: string, options?: BarcodeOptions): number[];
    image(image: Image, options?: ImageOptions): number[];
    linefeed(amount?: number): number[];
    lineSpacing(space?: number): number[];
    getSpacesForPercentage(percentage: number, textLength?: number, size?: TextSizeOption): number[];
    private font;
}
