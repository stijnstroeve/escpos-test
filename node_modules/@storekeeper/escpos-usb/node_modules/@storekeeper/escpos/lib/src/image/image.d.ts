import { ImageOptions } from '../escpos/commands/imageCommand';
export declare type LoadedImage = {
    data: number[];
    colors: number;
    width: number;
    height: number;
};
export declare abstract class ImageProvider {
    abstract load(url: string, options: ImageOptions): Promise<LoadedImage | null>;
}
export declare type ImageMetadata = {
    width: number;
    height: number;
    colors: number;
};
export declare type ImageData = {
    metadata: ImageMetadata;
    data: number[];
};
export declare type ImageBitmap = {
    data: any[];
    density: number;
};
export declare class Image {
    private imageProvider;
    private url;
    private options?;
    private data;
    constructor(imageProvider: ImageProvider, url: string, options?: ImageOptions);
    toBitmap(): ImageBitmap | null;
    load(): Promise<boolean>;
    private get cacheKey();
    static load(provider: ImageProvider, url: string, options?: ImageOptions): Promise<Image | null>;
}
