import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
import { AlignHTOptions } from '../escposBuilder';
export declare type FontOptions = 'A' | 'B';
export declare type TextSizeOption = 0.5 | 1 | 2 | 3 | 4;
export declare type SimpleTextOptions = {
    bold?: boolean;
    underline?: boolean;
};
export declare type TextOptions = {
    newLine?: boolean;
    breakOnWord?: boolean;
    size?: TextSizeOption;
} & SimpleTextOptions & AlignHTOptions;
export declare class TextCommand extends Command {
    text: string;
    options: TextOptions;
    key: string;
    constructor(text: string, options: TextOptions);
    build(compiler: EscPosCompiler): number[];
}
