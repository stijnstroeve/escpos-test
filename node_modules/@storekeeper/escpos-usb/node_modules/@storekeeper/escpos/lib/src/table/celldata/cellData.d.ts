import { CellDataType } from './cellDataType';
import { TableColumnDefinitionType, TableRow } from '../../escpos/commands/tableCommand';
import { TableCompiler } from '../tableCompiler';
export declare abstract class CellData<DataType = undefined> {
    type: CellDataType;
    data?: DataType;
    protected constructor(type: CellDataType, data?: DataType);
    abstract getTextLines<T, K extends keyof T>(row: TableRow<T>, column: TableColumnDefinitionType<T, K>, compiler: TableCompiler): string[];
}
