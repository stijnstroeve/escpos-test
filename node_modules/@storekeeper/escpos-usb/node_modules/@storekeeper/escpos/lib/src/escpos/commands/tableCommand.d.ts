import { Command } from './command';
import { EscPosCompiler } from '../escposCompiler';
import { SimpleTextOptions, TextSizeOption } from './textCommand';
export declare type TableColumnDefinitionType<T, K extends keyof T> = {
    key: K;
    width: number;
    header?: string;
    alignHTRows?: 'left' | 'right';
    alignHTHeader?: 'left' | 'right';
    bold?: boolean;
    headerBold?: boolean;
};
export declare type TableRow<T> = {
    data: T;
    size?: TextSizeOption;
    lineSpacing?: number;
    lineSpacingLineWrap?: number;
    line?: boolean;
} & SimpleTextOptions;
export declare type Table<T, K extends keyof T> = {
    rows: TableRow<T>[];
    columns: Array<TableColumnDefinitionType<T, K>>;
};
export declare class TableCommand<T, K extends keyof T> extends Command {
    table: Table<T, K>;
    key: string;
    constructor(table: Table<T, K>);
    build(compiler: EscPosCompiler): number[];
}
